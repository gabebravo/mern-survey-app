{
  "name": "mern-survey-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon index.js",
    "client": "nodemon start-client.js",
    "test": "export NODE_ENV=localtest || SET \"NODE_ENV=localtest\" && mocha server/**/*.test.js",
    "test-watch": "export NODE_ENV=localtest && nodemon --exec 'npm test'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^1.0.3",
    "body-parser": "^1.18.2",
    "dotenv": "^4.0.0",
    "express": "^4.15.5",
    "jsonwebtoken": "8.0.1",
    "mongoose": "^4.11.13"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
